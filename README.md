# Zio Lambda with AWS CDK

This repo contains code that

- Build Zio Layer
- Build Zio Example App
- Deploy Zio Layer
- Deploy Zio App

```zsh
npx aws-cdk deploy --region REGION --profile PROFILE --all
```
